using UnityEngine;

public class CandidateEnums : MonoBehaviour {
    public enum CandidateType {
        Human,
        NotHuman
    }

    public enum BodyLanguageType {
        Passive,
        Responsive
    }
    
}
